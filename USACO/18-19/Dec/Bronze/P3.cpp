#include <bits/stdc++.h>
using namespace std;

int amount;
vector<int> b1, b2;

void dfs(int day, int milk) {
	
}

int main() {
//	ifstream cin("backforth.in");
//	ofstream cout("backforth.out");
	ios::sync_with_stdio(0);
	cin.tie(0);
	for(int i=0; i<10; ++i) {
		int t;
		cin >> t;
		b1.push_back(t);
	}
	for(int i=0; i<10; ++i) {
		int t;
		cin >> t;
		b2.push_back(t);
	}
	
	
	return 0;
}
// 1e4 operations can brute force